//
// auto-generated by op2.py
//

void res_calc1_omp4_kernel(
  int *map0,
  int map0size,
  double *data0,
  int dat0size,
  double *data2,
  int dat2size,
  int *col_reord,
  int set_size1,
  int start,
  int end,
  int num_teams,
  int nthread){

  #pragma omp target teams num_teams(num_teams) thread_limit(nthread) \
    map(to:col_reord[0:set_size1],map0[0:map0size],data0[0:dat0size],data2[0:dat2size])
  #pragma omp distribute parallel for schedule(static,1)
  for ( int e=start; e<end; e++ ){
    int n_op = col_reord[e];
    int map0idx;
    int map1idx;
    map0idx = map0[n_op + set_size1 * 0];
    map1idx = map0[n_op + set_size1 * 1];

    //variable mapping
    double *q1 = &data0[1 * map0idx];
    double *q2 = &data0[1 * map1idx];
    double *res1 = &data2[1 * map0idx];
    double *res2 = &data2[1 * map1idx];

    //inline function
    

    double r1[1], r2[1];
    for(int i = 0; i < 1; i++){
        r1[i] = q1[i];
        r2[i] = q2[i];
        res1[i] += r1[i] + 1;
        res2[i] += r2[i] - 1;


    }
    //end inline func
  }

}
